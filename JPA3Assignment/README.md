For Ques 9 

an object from the cache can be cleared by calling the evict() method of 
session in which the object that has to be deleted is passed

For Ques 10
Transactional annotation help us to add transaction behaviour to the 
method i.e instead of creating new session for each query a single session
would be responsible for executing the queries defined in that method
inorder to avoid any data inconsistencies.

